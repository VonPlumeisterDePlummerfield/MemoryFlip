<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer rectangle with thick lines -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="16dp"
            android:color="#8f563b" />  <!-- Adjust the stroke color here -->
            <corners android:radius="0dp" />  <!-- Optional: Add rounded corners -->
        </shape>
    </item>


    <item android:top="20dp" android:left="20dp" android:right="20dp" android:bottom="20dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>
</layer-list>